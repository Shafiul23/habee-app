[pytest]
pythonpath = .
markers =
    unit: fast unit tests for pure logic/helpers.
    integration: API-level tests using Flask test client.
    contract: response contract tests for frontend payload safety.
    postgres: tests that should run against PostgreSQL in CI.
